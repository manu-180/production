pre-commit:
  parallel: false
  commands:
    biome-check:
      glob: "*.{ts,tsx,js,jsx,json,css}"
      run: npx @biomejs/biome check --write {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
